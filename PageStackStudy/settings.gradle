include ':app', ':appAidlService', ':appAidlClient', ':observerModel'

def include_foundation(String projectName, File projectDir) {
    include(projectName)
    project(projectName).projectDir = projectDir
}

include_foundation(':foundation-page-stack', file('dependence/foundation/page-stack'))